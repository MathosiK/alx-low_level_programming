#include "main.h"

/**
 * add - Check Main
 * @a: an integer a
 * @b: an integer
 * description: Function that adds two intergers and returns the result
 * Return: Results to add a and b
 */
int add(int a, int b)
{
	return (a + b);
}

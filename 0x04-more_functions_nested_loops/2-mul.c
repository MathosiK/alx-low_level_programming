#include "main.h"
/**
 * mul - checks for a digit (0 through 9)
 * @a: first int
 * @b: second int
 * Return: addition of the muliplication
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
